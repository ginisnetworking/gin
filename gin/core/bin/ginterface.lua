#!{PREFIX}/bin/lua

-- Load an interface into gin,
-- it the interface already exists it is updated with the changes.
-- The old definition is also kept, under the "old" identifier
-- so it can still be called by existing objects that reference it.
