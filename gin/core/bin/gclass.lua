#!{PREFIX}/bin/lua

-- Load a class into gin,
-- if the class already exists it is updated with the changes.
-- The old definition is also kept, under the "old" identifier
-- so it can still be called by existing objects of that class.


